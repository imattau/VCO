# VCO Relay Environment Configuration Example

# Path to the JSON configuration file (optional)
# VCO_CONFIG_PATH=/etc/vco/relay.json

# Port for the HTTP health check and metrics (default: 4000)
VCO_HTTP_PORT=4000

# Host for the HTTP health check (default: 127.0.0.1)
# Use ::1 for IPv6 loopback, or 0.0.0.0 / :: to listen on all interfaces
VCO_HTTP_HOST=127.0.0.1

# Libp2p listen addresses (comma-separated)
# Default: /ip4/0.0.0.0/udp/4001/quic-v1
# Example with IPv6: /ip4/0.0.0.0/udp/4001/quic-v1,/ip6/::/udp/4001/quic-v1
VCO_LISTEN_ADDRS=/ip4/0.0.0.0/udp/4001/quic-v1

# Directory for LevelDB storage and relay data
VCO_DATA_DIR=./relay-data

# Maximum number of concurrent libp2p connections
VCO_MAX_CONNECTIONS=256

# Proof of Work (PoW) Configuration
# Starting difficulty for new sessions
VCO_POW_DEFAULT_DIFFICULTY=0
# Maximum difficulty under high load
VCO_POW_MAX_DIFFICULTY=20

# Maximum storage size in MB (0 for unlimited)
VCO_MAX_STORE_SIZE_MB=0
